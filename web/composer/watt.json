{
  "$schema": "https://schemas.platformatic.dev/@platformatic/composer/2.74.1.json",
  "composer": {
    "refreshTimeout": 1000,
    "services": [
      { "id": "api", "proxy": { "prefix": "/api" } },
      { "id": "db", "proxy": { "prefix": "/db" } },
      { "id": "app", "proxy": { "prefix": "/app" } }
    ]
  },
  "watch": true
}
